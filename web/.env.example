# Production API base — point at your deployed FastAPI backend (Render/Railway/Fly).
# Leave blank in development (vite proxies /api → http://localhost:8000).
VITE_API_BASE=https://your-backend.onrender.com
